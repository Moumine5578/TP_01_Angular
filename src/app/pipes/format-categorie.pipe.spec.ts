import { FormatCategoriePipe } from './format-categorie.pipe';

describe('FormatCategoriePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCategoriePipe();
    expect(pipe).toBeTruthy();
  });
});
